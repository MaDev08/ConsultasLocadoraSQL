1-) SELECT FILME.COD_FILME, COUNT(LOCACOES.COD_FILME) AS QUANTIDADE_ALUGADO FROM dbo.FILME INNER JOIN dbo.LOCACOES ON dbo.FILME.COD_FILME = dbo. LOCACOES.COD_FILME GROUP BY dbo.FILME.COD_FILME ORDER BY QUANTIDADE_ALUGADO DESC; 

2-) SELECT FILME.FILME, LOCACOES.DATA_DEVOLUCAO FROM FILME INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME INNER JOIN CLIENTES ON LOCACOES.COD_CLIENTE = CLIENTES.COD_CLIENTE INNER JOIN CATEGORIA ON FILME.COD_CATEGORIA = CATEGORIA.COD_CATEGORIA WHERE CATEGORIA.NOME_CATEGORIA = 'Ação' AND FILME.DIRETOR = 'Richard Donner' AND CLIENTES.NOME = 'Edson Martin Feitosa'; 

3-) SELECT FILME.FILME, FILME.VALOR_LOCACAO, FILME.VALOR_LOCACAO * 0.10 AS DESCONTO, FILME.VALOR_LOCACAO - (FILME.VALOR_LOCACAO * 0.10) AS VALOR_FINAL FROM FILME; 

4-) SELECT DISTINCT FILME.FILME FROM FILME INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME INNER JOIN CLIENTES ON LOCACOES.COD_CLIENTE = CLIENTES.COD_CLIENTE WHERE CLIENTES.CIDADE = 'Votorantim'; 

5-) SELECT FILME.FILME FROM FILME INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME WHERE CAST(LOCACOES.DATA_RETIRADA AS DATE) = CAST(GETDATE() AS DATE); 

6-) SELECT SUM(FILME.VALOR_LOCACAO) AS TOTAL_LUCRO FROM FILME INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME WHERE FILME.FILME = 'A vida é bela'; 

7-) SELECT FILME.DIRETOR, COUNT(LOCACOES.COD_FILME) AS TOTAL_SAIDAS FROM FILME INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME GROUP BY FILME.DIRETOR; 

8-) SELECT DISTINCT CATEGORIA.NOME_CATEGORIA FROM CATEGORIA INNER JOIN FILME ON CATEGORIA.COD_CATEGORIA = FILME.COD_CATEGORIA INNER JOIN LOCACOES ON FILME.COD_FILME = LOCACOES.COD_FILME INNER JOIN CLIENTES ON LOCACOES.COD_CLIENTE = CLIENTES.COD_CLIENTE WHERE CLIENTES.NOME = 'Maria Chiquinha'; 

9-) SELECT DISTINCT CLIENTES.NOME, CLIENTES.TELEFONE FROM CLIENTES INNER JOIN LOCACOES ON CLIENTES.COD_CLIENTE = LOCACOES.COD_CLIENTE INNER JOIN FILME ON LOCACOES.COD_FILME = FILME.COD_FILME WHERE FILME.FILME = 'Constantine'; 

10-) SELECT CATEGORIA.NOME_CATEGORIA FROM CATEGORIA LEFT JOIN FILME ON CATEGORIA.COD_CATEGORIA = FILME.COD_CATEGORIA WHERE FILME.COD_FILME IS NULL; 

 

 